<h2 class="page_name"><%= (t "show_users")%></h2>
	
  <%= paginated_collection_content(@users, @table_id, [(t "attributes.id"), (t "attributes.email"), (t "attributes.general_actions")]) %>
  <%= paginate_footer(@users) %>
