<%= simple_form_for(@class_type) do |f| %>
  <% if @class_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@class_type.errors.count, "error") %> prohibited this class_type from being saved:</h2>

      <ul>
      <% @class_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit :value => "Save" %>
  </div>
<% end %>
